server:
  port: 8092
  mode: debug

database:
  driver: postgres
  postgres_dsn: "host=127.0.0.1 user=postgres password=postgres dbname=supplycore port=5432 sslmode=disable TimeZone=Asia/Shanghai"

auth:
  enabled: true
  jwt_secret: "change-me-in-production-use-long-random-string"

integrations:
  productcore_api_url: "http://127.0.0.1:8090"
  warehousecore_api_url: "http://127.0.0.1:8095"
  ordercore_api_url: "http://127.0.0.1:8098"

storage:
  driver: minio                  # local | minio
  prefix: "attachments"
  local_path: "./data/uploads"   # driver: local 时生效
  public_base_url: "http://127.0.0.1:9100/supplycore"
  minio:
    endpoint: "127.0.0.1:9100"
    access_key: "minioadmin"
    secret_key: "minioadmin"
    bucket: "supplycore"
    use_ssl: false
    prefix: "attachments"
    public_read: true

cors:
  allow_origins:
    - "http://localhost:5175"
    - "http://127.0.0.1:5175"
    - "http://localhost:5174"
    - "http://127.0.0.1:5174"
    - "http://localhost:5173"
    - "http://127.0.0.1:5173"
